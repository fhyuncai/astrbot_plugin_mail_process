{
  "mail_accounts": {
    "type": "template_list",
    "description": "邮箱账户列表",
    "hint": "添加你要监控的邮箱账户，支持 Gmail、杭电邮箱、QQ 邮箱等所有支持 IMAP 的邮箱",
    "templates": {
      "imap_account": {
        "name": "IMAP 邮箱账户",
        "hint": "填写邮箱的 IMAP 连接信息",
        "items": {
          "name": {
            "description": "账户备注名",
            "type": "string",
            "default": "",
            "hint": "例如：谷歌邮箱、QQ邮箱、工作邮箱"
          },
          "imap_server": {
            "description": "IMAP 服务器地址",
            "type": "string",
            "default": "",
            "hint": "例如：imap.gmail.com、imap.qq.com"
          },
          "imap_port": {
            "description": "IMAP 端口",
            "type": "int",
            "default": 993,
            "hint": "SSL 连接通常为 993"
          },
          "email": {
            "description": "邮箱地址",
            "type": "string",
            "default": ""
          },
          "password": {
            "description": "密码或应用专用密码",
            "type": "string",
            "default": "",
            "hint": "当邮箱服务要求时，请填写授权码或应用专用密码"
          },
          "smtp_server": {
            "description": "SMTP 服务器地址",
            "type": "string",
            "default": "",
            "hint": "例如：smtp.gmail.com、smtp.qq.com"
          },
          "smtp_port": {
            "description": "SMTP 端口",
            "type": "int",
            "default": 465,
            "hint": "SSL 常用 465，STARTTLS 常用 587"
          },
          "smtp_use_ssl": {
            "description": "SMTP 使用 SSL 连接",
            "type": "bool",
            "default": true,
            "hint": "为 false 时将使用 STARTTLS"
          },
          "smtp_password": {
            "description": "SMTP 密码或应用专用密码",
            "type": "string",
            "default": "",
            "hint": "留空时默认复用上方的 IMAP 密码"
          },
          "use_ssl": {
            "description": "IMAP 使用 SSL 连接",
            "type": "bool",
            "default": true
          }
        }
      }
    }
  },
  "check_interval": {
    "description": "检查间隔（分钟）",
    "type": "int",
    "default": 5,
    "hint": "每隔多少分钟检查一次新邮件，建议不低于 2 分钟"
  },
  "admin_uids": {
    "description": "管理员 UID 列表",
    "type": "list",
    "default": [],
    "hint": "只有列表中的用户可以使用插件命令，且插件会向这些管理员最近一次和机器人的会话发送通知"
  },
  "ai_summary": {
    "description": "启用 AI 摘要",
    "type": "bool",
    "default": false,
    "hint": "启用后将先生成简短摘要，再发送邮件通知"
  },
  "enable_ai_processing": {
    "description": "启用 AI 邮件处理",
    "type": "bool",
    "default": false,
    "hint": "开启后，新邮件会先交给 AI 判断是否需要通知管理员或发起回复"
  },
  "ai_processing_prompt": {
    "description": "AI 邮件处理提示词",
    "type": "string",
    "default": "你收到了一封新邮件，不要回复，请阅读后决定是否通知，如果是广告邮件或垃圾邮件请直接忽略。",
    "hint": "会和邮件主题、发件人、正文等上下文一起发送给 AI"
  },
  "ai_allow_notify": {
    "description": "允许 AI 通知管理员",
    "type": "bool",
    "default": true,
    "hint": "开启后，AI 可以决定将邮件通知发给管理员"
  },
  "ai_allow_send_mail": {
    "description": "允许 AI 发送邮件",
    "type": "bool",
    "default": true,
    "hint": "开启后，AI 可以发送邮件；自动处理新邮件时可自动回复，聊天场景下需先询问并确认"
  },
  "blacklist_settings": {
    "description": "黑名单设置",
    "type": "object",
    "items": {
      "enable": {
        "description": "启用黑名单过滤",
        "type": "bool",
        "default": false,
        "hint": "开启后，命中发件人、主题或正文任意黑名单规则的邮件不会推送"
      },
      "sender_rules": {
        "description": "发件人规则",
        "type": "list",
        "default": [],
        "hint": "支持完整邮箱、@域名后缀，或普通关键词包含匹配"
      },
      "subject_rules": {
        "description": "主题规则",
        "type": "list",
        "default": [],
        "hint": "使用不区分大小写的包含匹配"
      },
      "body_rules": {
        "description": "正文规则",
        "type": "list",
        "default": [],
        "hint": "使用不区分大小写的包含匹配"
      }
    }
  },
  "whitelist_settings": {
    "description": "白名单设置",
    "type": "object",
    "items": {
      "enable": {
        "description": "启用白名单过滤",
        "type": "bool",
        "default": false,
        "hint": "开启后，只有命中至少一条白名单规则的邮件才会推送"
      },
      "sender_rules": {
        "description": "发件人规则",
        "type": "list",
        "default": [],
        "hint": "支持完整邮箱、@域名后缀，或普通关键词包含匹配"
      },
      "subject_rules": {
        "description": "主题规则",
        "type": "list",
        "default": [],
        "hint": "使用不区分大小写的包含匹配"
      },
      "body_rules": {
        "description": "正文规则",
        "type": "list",
        "default": [],
        "hint": "使用不区分大小写的包含匹配"
      }
    }
  },
  "max_body_length": {
    "description": "通知预览最大长度",
    "type": "int",
    "default": 500,
    "hint": "通知中展示的正文字符上限，也会用于 AI 摘要输入"
  },
  "filter_body_length": {
    "description": "正文过滤最大长度",
    "type": "int",
    "default": 3000,
    "hint": "用于正文黑白名单匹配的最大字符数"
  },
  "mail_query_max_items": {
    "description": "AI 查询邮件最大条数",
    "type": "int",
    "default": 50,
    "hint": "mail_query 工具最多会读取最近多少封邮件，建议不高于 100"
  },
  "mail_query_page_size": {
    "description": "AI 查询默认每页条数",
    "type": "int",
    "default": 10,
    "hint": "mail_query 工具默认每页返回多少封邮件，最大 20"
  },
  "mail_query_preview_length": {
    "description": "AI 查询摘要长度",
    "type": "int",
    "default": 120,
    "hint": "mail_query 返回的邮件摘要最大长度，避免列表过长"
  },
  "mail_read_body_length": {
    "description": "AI 读信正文最大长度",
    "type": "int",
    "default": 4000,
    "hint": "mail_read 返回的正文最大长度，避免单封邮件过长"
  }
}
